using System;

namespace TestingShitHere
{
    using System.Linq;
    using System.Reflection;
    using Papers;
    using Papers.Services;

    class Kur
    {
        // static void Main(string[] args)
        // {
        //     var allClasses = Assembly.GetAssembly(typeof(Program)).DefinedTypes;
        //     var inheritIService = allClasses.Where(c => c.IsInterface && c.ImplementedInterfaces.Contains(typeof(IService)));
        //
        //     var concreteTypes = allClasses.Where(c => c.IsClass && c.Name == inheritIService.FirstOrDefault().Name.Substring(1));
        // }
    }
}